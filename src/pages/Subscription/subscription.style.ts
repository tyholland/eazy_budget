import styled from "styled-components";
import { RUBIK } from "../../index.style.ts";

export const Title = styled.div`
  font-size: 30px;
  font-weight: 700;
  font-family: ${RUBIK};
  margin-bottom: 30px;
`;
